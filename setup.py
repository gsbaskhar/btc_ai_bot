"""Compatibility entry point.

This file deliberately does not create or overwrite project files. Install the
dependencies from requirements.txt, configure config.py, then run main.py.
"""
if __name__ == "__main__":
    print("Install dependencies: pip install -r requirements.txt")
    print("Configure config.py, log in to MT5, then run: python main.py")
